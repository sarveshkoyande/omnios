fastapi==0.139.0
uvicorn[standard]==0.50.0
pydantic==2.13.4
requests==2.34.2
openpyxl==3.1.5
anthropic==0.116.0
azure-identity==1.25.3
pytrends==4.9.2
python-multipart==0.0.32
# Postgres driver for the dual-dialect DB layer (strategy/db.py). Only imported when
# DATABASE_URL is set to a postgres:// URL; the default SQLite path never loads it.
psycopg[binary]>=3.2
pypdf==6.14.2
python-docx==1.2.0
python-pptx==1.0.2
reportlab==4.2.5
playwright==1.48.0
# Standalone memory layer (strategy/memory_cognee.py). litellm is pinned below cognee's
# own floor because 1.92.0 ships no wheel and its sdist needs a Rust/maturin toolchain
# not present in this environment; 1.91.3 has a normal wheel and works fine.
cognee==1.3.0
litellm==1.91.3
fastembed==0.8.0
