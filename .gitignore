# --- secrets / local env ---
.env
.env.*

# --- runtime data store (SQLite + blobs, reseeded from assets/seed + config) ---
/data/

# --- knowledge base: too large for git; now lives in Postgres (loaded once via
#     strategy/load_kb_to_pg.py). Kept locally for SQLite-only dev, but never committed. ---
/assets/seed/omni_kb.db

# --- python ---
__pycache__/
*.py[cod]
*.egg-info/
.mypy_cache/
.pytest_cache/
.ruff_cache/
.venv/
venv/

# --- node / frontend ---
node_modules/
.vite/
*.tsbuildinfo

# --- logs / scratch ---
.codex-logs/
*.log
plan_out.html

# --- os ---
.DS_Store
Thumbs.db
*.pem
